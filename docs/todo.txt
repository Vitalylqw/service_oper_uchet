1. Проблема - в БД поле read_deals.invoice_date - varchar. Должно быть дата. Необходимо
    1. Изменить струткутру даных
    2. Измениеить логику загрузки данных, что бы поле форматировалось и хранилось как дата.
    3. Внести изменения в текущи еданные БД - изменить тип данных этого поля
    4. ПРивести в соответсвие api
    5 Разобраться с fronted, что бы там работало с новым api
    6.Проверить код, где еще нужно актуализировать, в связи с изменеие формата этого поля

2. Проветь, почему в разных местах  saller и seller . Выяснить причину. При необходимости привести к одному виду. Найти возможные проблемы в этой связи и сиправтьь их

3. Избавиться от полей с currency_ammount. Достаточно обычных полей. Учет в валюте не ведется.

4. Не работает вкладка синхронизация



 5. Полное тестирование на реальных данных


    Я бы хотел, оттестировать его на небольшой выборке из реальных данных в файле data\real_data_for_testing\Data_source_excel.xlsx.
    Мне нужно получить скрипт, с помощью которого, можно было бы проверить весь цикл работы проекта, проверить каждую функциональность, каждый метод и функцию. Что они работают без ошибок и возвращают, нужные данные. А проект в целом выполняется без ошибок и правильно решает  свои задачи. В том числе фронтед
    В проекте имеется тестовая база данных на sqlite, в которой можно тренироваться - data\service_oper_uchet.sqlite

    Цель проекта обрабатывать файлы Эксель (файл оперучет, который ведется в реальном времени - в него вносят новые данные, изменяю старые и удаляют не нужные).
    Нужно синхронизировать его с БД.
    Для тестирование синхронизации наврное приедеться создавать еще несколько файлов на основании этого, что бы имитировать добавление новых данные, изменения старых, а так же удаление некоторых данных. Так же убедиться , что он не дублирует данные.
    На каждом этапе нужно проверять что происходит, детально легировать происходящее. Так проверить работу фронтенда
    Авторизацию, чтобы открывались все страницы и на них отображалась правильная информация. Работали как от них требуется все кнопки, фильтры 